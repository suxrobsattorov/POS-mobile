import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('POS Mobile smoke test', (WidgetTester tester) async {
    // Basic smoke test — app initializes without crashing
    expect(true, isTrue);
  });
}
